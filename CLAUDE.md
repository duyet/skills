# @duyet/skills - Claude Code Skills Package

> Production-quality skills for Claude Code agents

## Quick Links

- `@AGENTS.md` - Agent definitions and coordination patterns
- `@README.md` - Package documentation and installation
- `@./scripts/` - Automation scripts for maintenance

## Installation

```bash
bunx skills add duyet/skills
```

## Versioning Rules

Follow semantic versioning (semver) for all skills:

| Change Type | Version Bump | Example |
|-------------|--------------|---------|
| Bug fix, docs | Patch | 1.0.0 → 1.0.1 |
| New skill, minor changes | Minor | 1.0.0 → 1.1.0 |
| Breaking change | Major | 1.0.0 → 2.0.0 |

**When to bump:**
- Adding new skills → **Minor**
- Modifying existing skill behavior → **Minor** (or Major if breaking)
- Updating documentation only → **Patch**
- Bug fixes → **Patch**

Update `package.json` version when making changes.

## Skill Structure

Each skill follows the skills.sh convention:

```
skill-name/
└── SKILL.md          # Frontmatter + instructions
```

### SKILL.md Format

```markdown
---
name: skill-name
description: Brief description for skill selection
---

# Skill Name

Detailed instructions for the agent.

## When to use

Describe when this skill should be activated.

## Instructions

Step-by-step instructions or patterns.
```

## Maintenance Scripts

### List all skills
```bash
bun run list
```

### Format skill files
```bash
bun run format
```

### Validate structure
```bash
bun run validate
```

### Generate README
```bash
bun run generate:readme
```

## Adding a New Skill

1. Create new directory: `mkdir skills/new-skill`
2. Create `SKILL.md` with frontmatter
3. Run validation: `bun run validate`
4. Update README: `bun run generate:readme`
5. Commit: `git commit -m "feat(skills): add new-skill"`

## Removing a Skill

1. Remove directory: `rm -rf skills/old-skill`
2. Update README: `bun run generate:readme`
3. Bump patch version in `package.json`
4. Commit: `git commit -m "fix(skills): remove old-skill"`

## Commit Convention

Use semantic commits with `skills` scope:

```
feat(skills): add new-skill
fix(skills): fix bug in existing-skill
docs(skills): update documentation
```

Co-author: `Co-Authored-By: duyetbot <duyetbot@users.noreply.github.com>`

---

**Generated by @duyet/skills v1.0.0**
